# Introduction

This small tool will allow developers to easily integrate Web Content to be reloaded when some files are watched, changed and the web page must be reloaded.
It will offer a simple web BE which will serve a websocket that the only purpose would be to tell the FE that changes appeared and the FE must reload. 
It will also provide the FE code snipped needed to run in the Web Content to start and watch on changes. 